<%= form_with(model: event_class, local: true) do |form| %>
  <% if event_class.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(event_class.errors.count, "error") %> prohibited this event_class from being saved:</h2>

      <ul>
      <% event_class.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
