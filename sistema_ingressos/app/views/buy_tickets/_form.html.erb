<%= form_with(model: buy_ticket, local: true) do |form| %>
  <% if buy_ticket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(buy_ticket.errors.count, "error") %> prohibited this buy_ticket from being saved:</h2>

      <ul>
      <% buy_ticket.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :bought_moment %>
    <%= form.datetime_select :bought_moment %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
