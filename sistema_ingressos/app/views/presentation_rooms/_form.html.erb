<%= form_with(model: presentation_room, local: true) do |form| %>
  <% if presentation_room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(presentation_room.errors.count, "error") %> prohibited this presentation_room from being saved:</h2>

      <ul>
      <% presentation_room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
